<p style="color: green"><%= notice %></p>

<% if @review.present? %>
  <%= render partial: "reviews/review_detail", locals: { review: @review } %>
<% else %>
  <p>レビューが見つかりません。</p>
  <%= link_to "Back", reviews_path %>
<% end %>